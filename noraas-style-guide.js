/* STRINGS:
~All strings, except those used as object key definitions must be enclosed in
backtick single quotes. If you must use quote for declaring object key, such as
when needing to assign the key name as an operator such as '+' or '*'. In this case,
 strings will be stored in single quotes.
~
